<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.4.xsd">

    <changeSet id="create-users-table" author="your-name">
        <createTable tableName="users" remarks="Таблица пользователей системы (заказчики и исполнители)">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="email" type="varchar(255)">
                <constraints unique="true" nullable="false"/>
            </column>

            <column name="is_email_verified" type="boolean" defaultValue="false">
                <constraints nullable="false"/>
            </column>

            <column name="phone_number" type="varchar(20)">
                <constraints nullable="true"/>
            </column>

            <column name="is_phone_number_verified" type="boolean" defaultValue="false">
                <constraints nullable="false"/>
            </column>

            <column name="password" type="varchar(255)">
                <constraints nullable="false"/>
            </column>

            <column name="role" type="varchar(50)">
                <constraints nullable="false"/>
            </column>

            <column name="cost_per_month" type="decimal(10,2)" defaultValue="0.00">
                <constraints nullable="false"/>
            </column>

            <column name="city_id" type="bigint">
<!--                <constraints-->
<!--                        nullable="true"-->
<!--                        foreignKeyName="fk_users_city_id"-->
<!--                        references="cities(id)"/>-->
            </column>

            <column name="auth_token" type="varchar(512)">
                <constraints nullable="true"/>
            </column>

            <column name="created_at" type="timestamp" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="timestamp" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="deleted_at" type="timestamp">
                <constraints nullable="true"/>
            </column>
        </createTable>
    </changeSet>

</databaseChangeLog>

